import { ActivationDirective } from './activation.directive';

describe('ActivationDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivationDirective();
    expect(directive).toBeTruthy();
  });
});
